require 'spec_helper'

describe EnDeCrypt do
  it 'has a version number' do
    expect(EnDeCrypt::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
